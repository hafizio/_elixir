defmodule BinarySearch do

  @doc"""
  base case
  """
  def call([], n), do: {:error, "#{n} Not Found"}

  @doc"""
  recursive case

  1. Odd or even case?

  """
  def call(list, n) do
    middle = Enun
    case 
  end

  @doc"""
  1. Return index if the n is the middle element in the list
  """
  defp divide do
  end

  defp middle_element(list) do
    Enum.count(list)
  end

end
